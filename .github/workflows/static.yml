# GitHub Pages configuration
# This file tells GitHub Pages to serve from the dist directory
# and enables Jekyll processing (though we're using static HTML)

# Enable Jekyll processing for better GitHub Pages integration
# This allows us to use GitHub Pages features like custom 404 pages 